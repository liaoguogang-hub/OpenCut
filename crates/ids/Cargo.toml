[package]
name = "ids"
description = "Zero-dependency opaque identity types for OpenCut editor-core entities"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = "1"
